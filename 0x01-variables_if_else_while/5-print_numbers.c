#include <stdio.h>

/**
 * main - entry point
 *
 * Description: 'this program prints the alphapit in lower case'
 *
 * Return: return 0 and exit
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
