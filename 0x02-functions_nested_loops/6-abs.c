#include "main.h"
#include <stdlib.h>

/**
 * _abs - coputes the absolute value of an integer
 *
 * @n: is an integer number
 *
 * Return: the function return rhe abs value of n
 */

int _abs(int n)
{
	return (abs(n));
}
